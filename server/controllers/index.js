let authController =  require('./auth');
let githubController =  require('./github');
let dashboardController = require('./dashboard');

module.exports = {
    authController,
    githubController,
    dashboardController
}